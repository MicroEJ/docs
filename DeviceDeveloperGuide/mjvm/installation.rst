Installation
============

The MJVM and its components are mandatory. In the platform configuration
file, check ``GROUP.KF`` to install the MJVM in "Multi applications"
mode. Otherwise, the "Single application" mode is installed.
